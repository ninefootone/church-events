name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create release zip
        run: |
          cd ..
          # IMPORTANT: zip root folder must remain "church-events/" — PUC matches
          # the folder name to the plugin slug. Do not rename or restructure.
          zip -r church-events.zip church-events/ \
            --exclude "*.git*" \
            --exclude "*/.github/*" \
            --exclude "*/node_modules/*" \
            --exclude "*/.DS_Store" \
            --exclude "church-events/lib/**/.git" \
            --exclude "church-events/lib/**/.git/*"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ../church-events.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
